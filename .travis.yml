language: clojure

jdk:
- openjdk8

services:
  - docker

sudo: required

env:
  global:
  - COMMIT=${TRAVIS_COMMIT::8}
  - DB_HOST="localhost"
  - DB_KEYSPACE="nemo_test"
  - HTTP_PORT="5757"

before_install:
  - sudo sysctl -w vm.max_map_count=262144
  - make deps-up-d
  - sudo apt-get update -qq

after_success:
  - ./bin/build

notifications:
  slack:
    rooms:
      - lcmap:UTqlh9PfPVomfpli10WKyZoh#cicd
